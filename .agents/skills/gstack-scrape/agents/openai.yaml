interface:
  display_name: "gstack-scrape"
  short_description: "Pull data from a web page through the Aside browser — your real, already signed-in sessions. Read-only; returns one..."
  default_prompt: "Use gstack-scrape for this task."
policy:
  allow_implicit_invocation: true
